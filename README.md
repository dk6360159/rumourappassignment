# rumourIn
In this we project we tried make it more generaliged project for future updates in this app, Not just for Firestore database but i designed this project for https servers, aws or anything.


In This project i focused more on writing code of data handling in the back of the UI Screens.
Error or Failure handing i error folder in Core we made generaliged exceptions we get in a Flutter app.
after that respective failure we convert those exceptions in failures.

For Statemanagment i started with writing Base Service, Module generaliged class.
In this app we have multiple modules so that, Module class help us such that each module initialized only once.
As of now in this service we have only one service chat service, but as we make it more usefull app we add more service like , authentication and all i made sure that future updates become very easy now

